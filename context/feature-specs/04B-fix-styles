    * We want to fix the style of the home screen.  Review the screenshot and update the UI of our current application to look more like the one in the screenshot. It means a 50/50 left and right side layout with some color contrast between the left and right side. We also need to fix the fonts so that it uses the ones outlined in our UI guidelines. Also fix the icons for the bullets on the left side.