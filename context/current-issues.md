CURRENT ISSUES:

1.  when I signout using the UserButton, the URL switches to localhost:3000 which is the signin page, but I have a blank page with a "Rendering" message at the bottom. If I reload the page at that point, I get the signin page.  